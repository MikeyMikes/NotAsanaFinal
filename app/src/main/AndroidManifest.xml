<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brosh.notasana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tasks.SetupTaskActivity" />
        <activity android:name=".Tasks.TaskDetailActivity" />
        <activity android:name=".Tasks.ChangeTaskActivity" />
        <activity android:name=".Tasks.TaskTab" />
        <activity android:name=".Friends.FriendsTab" />
        <activity android:name=".GroceryTab" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Friends.AddFriendActivity"></activity>
    </application>

</manifest>